import { PressHerePipe } from './press-here.pipe';

describe('PressHerePipe', () => {
  it('create an instance', () => {
    const pipe = new PressHerePipe();
    expect(pipe).toBeTruthy();
  });
});
